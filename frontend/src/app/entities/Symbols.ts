export interface ISymbol {
  symbol: string;
  basePrecision: number;
  quotePrecision: number;
  minNotional: string;
  minLotSize: string;
  isFavorite: boolean;
  createdAt: string;
  updatedAt: string;
}